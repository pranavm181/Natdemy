import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const primary = Color(0xFF582DB0);
  static const primaryLight = Color(0xFF8B5CF6); // Lighter purple for gradients
  static const secondary = Color(0xFF03DAC6); // Placeholder
  static const accent = Color(0xFFA1C95C); // Greenish accent from splash
  
  // Backgrounds
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  
  // Text
  static const textPrimary = Color(0xFF1E293B);   // Slate 800
  static const textSecondary = Color(0xFF64748B); // Slate 500
  static const textDark = Color(0xFF000000);
  static const textLight = Color(0xFFFFFFFF);
  
  // Functional
  static const error = Color(0xFFEF4444); // Slate red
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  
  // UI Elements
  static const divider = Color(0xFFE2E8F0);
  static const border = Color(0xFF582DB0); // Matching primary as per main.dart
  static const inputFill = Color(0xFFFFFFFF);
  
  // Brand Specific
  static const whatsapp = Color(0xFF25D366);
  
  // Shadows
  static final shadow = Colors.black.withOpacity(0.15);
}
